function checkNumberCriteria() {

  let num = 8;
  let message = "";

  if (num > 0 && num % 2 === 0) 
  {
    message = "The number is positive and even.";
  }
  else 
  {
    message = "The number does not meet the criteria.";
  }


  return message;
}

console.log(checkNumberCriteria());
